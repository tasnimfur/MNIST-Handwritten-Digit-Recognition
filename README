Instructions on how to run the code:

1) Download the code
2) Download keras module, numpy, and opencv if they are not downloaded on your computer.
3) To run the file, open a terminal window and navigate to the folder where the file is downloaded and execute code using the following command:
    > python3 Digit_Recognizer.py
